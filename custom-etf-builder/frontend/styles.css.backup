* { box-sizing: border-box; }
body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif; margin: 0; padding: 24px; background: #0b1220; color: #e6edf3; }
.container { max-width: 1100px; margin: 0 auto; }
h1 { margin: 0 0 24px; font-size: 24px; }
.card { background: #0f172a; border: 1px solid #1e293b; border-radius: 12px; padding: 16px; margin-bottom: 16px; }
label { display: block; font-size: 14px; margin-bottom: 8px; color: #b6c2cf; }
textarea { width: 100%; min-height: 90px; resize: vertical; border-radius: 8px; border: 1px solid #334155; background: #0b1220; color: #e6edf3; padding: 10px; }
input[type="number"], input[type="text"] { width: 100%; border-radius: 8px; border: 1px solid #334155; background: #0b1220; color: #e6edf3; padding: 10px; }
.risk-group { display: flex; gap: 16px; align-items: center; }
button { margin-top: 12px; padding: 10px 16px; background: #2563eb; color: white; border: none; border-radius: 8px; cursor: pointer; }
button:hover { background: #1d4ed8; }
.row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
.col { }
#error { margin-top: 10px; background: #7f1d1d; color: #fecaca; padding: 10px; border-radius: 8px; }
.results table { width: 100%; border-collapse: collapse; }
.results th, .results td { border-bottom: 1px solid #1e293b; padding: 8px; text-align: left; font-size: 14px; }
.badges { display: flex; gap: 8px; flex-wrap: wrap; margin: 12px 0; }
.badges .badge { background: #1e293b; border: 1px solid #334155; color: #93c5fd; padding: 4px 8px; border-radius: 999px; font-size: 12px; }
.meta { display: grid; grid-template-columns: repeat(2, 1fr); gap: 8px; font-size: 13px; color: #b6c2cf; }
.disclaimer { font-size: 12px; color: #94a3b8; margin-top: 12px; }
